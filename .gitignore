# Python / uv
.venv/
__pycache__/
*.py[cod]

# uv keeps uv.lock tracked for reproducible installs - do not ignore it.

# Ansible
*.retry
# Local cache of roles/modules/collections (e.g. created by ansible-lint).
.ansible/
# ansible-lint cache.
.cache/

# macOS
.DS_Store
